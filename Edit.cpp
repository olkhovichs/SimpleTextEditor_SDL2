#include <iostream>

#include <SDL.h>
#include <SDL_ttf.h>

#include "Interface.h"
#include "Edit.h"

/*int Edit::inputText(SDL_Renderer*& renderer, const char* message, const char* styleText,
	int size, SDL_Color color, SDL_Rect rect) {

	SDL_Event e;
	bool quit = true;
	//std::vector<std::string> mainBuffer;
	std::string mainBuffer = "Some text";
	
}*/